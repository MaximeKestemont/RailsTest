<!DOCTYPE html>
<html>
<head>
  <title>Todo</title>
  <%= stylesheet_link_tag    'application', media: 'all' %>
  <%= javascript_include_tag 'application' %>
  <%= csrf_meta_tags %>
</head>

<body>

<div class="row-fluid">
  <div class="span10 offset1">
    <div class="hero-unit text-center">
      <h1>ToDo</h1>
      <p>Welcome to the tutorial's ToDo application</p>
      <strong><%= link_to 'New task', new_task_path, class: 'btn btn-primary', remote: true %></strong>
      <div class="text-center"><strong><%= current_user.email %></strong></div>
      <div class="text-center"><strong><%= link_to 'Sign out', destroy_user_session_path, :method => :delete, class: 'btn btn-primary' %></strong></div>
    </div>
  </div>
</div>


<%= yield %>

</body>
</html>
	